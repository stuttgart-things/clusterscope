apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# base/ is populated by: task deploy-kustomize (pulls OCI artifact)

resources:
  - base
  - httproute.yaml

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: clusterscope
